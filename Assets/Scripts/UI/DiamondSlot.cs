using UnityEngine;

/// <summary>
/// Component để đánh dấu slot là DiamondSlot (phân biệt với slot inventory/hotbar)
/// </summary>
public class DiamondSlot : MonoBehaviour
{
    // Component này chỉ để đánh dấu, không cần logic gì
    // Có thể dùng để kiểm tra slot có phải là diamond slot không
}

